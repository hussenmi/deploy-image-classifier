# deploy-image-classifier
In this repository, I use different custom convolutional neural network architectures for image classification on the CIFAR-10 dataset. I use MLFlow to log the training process and compare different models. I then choose the best model and deploy it using Flask. This application can be accessed using both an interactive web page and an API
